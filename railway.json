{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && node node_modules/vite/bin/vite.js build && cp serve.json dist/serve.json"
  },
  "deploy": {
    "startCommand": "npx serve dist",
    "restartPolicyType": "ON_FAILURE"
  }
}
